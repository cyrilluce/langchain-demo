# langchain-demo monorepo

Repository skeleton for a minimal LLM Agent demo:

- `server/` — Python FastAPI app integrating LLM orchestration examples using `langchain` / `langgraph`.
- `ui/` — Bun + React frontend that calls the server's `/agent` endpoint.

See `specs/1-monorepo-llm-agent/spec.md` for feature details.
