# ui

Bun + React demo UI that calls the server `POST /agent` endpoint.

Quick start

1. Install Bun (https://bun.sh/) if not installed.
2. From the `ui/` folder run:

```bash
bun install
bun dev
```

3. Open `http://localhost:3000` (or the port printed by Bun) and use the UI to send prompts.
